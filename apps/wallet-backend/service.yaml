apiVersion: v1
kind: Service
metadata:
  name: wallet-backend
  namespace: wallet
  labels:
    app.kubernetes.io/name: wallet-backend
    app.kubernetes.io/part-of: woori-wallet
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: wallet-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
